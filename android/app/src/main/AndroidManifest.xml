<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TenderBaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <!-- FileProvider for sharing and opening downloaded PDFs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Onboarding / Splash -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (Tender discovery: search, filters, sort) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />

        <!-- Tender Detail Activity (deep-linkable: tenderbase://tender/{id}) -->
        <activity
            android:name=".DetailActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tenderbase" android:host="tender" />
            </intent-filter>
        </activity>

        <!-- Deadline command centre (Sprint 4) -->
        <activity
            android:name=".DeadlinesActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Saved Tenders Activity -->
        <activity
            android:name=".SavedTendersActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Saved Searches Activity (Sprint 2 alerts) -->
        <activity
            android:name=".SavedSearchesActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Downloaded Documents Activity -->
        <activity
            android:name=".DownloadsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Notification History Activity -->
        <activity
            android:name=".NotificationsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Categories & Provinces Preferences Activity -->
        <activity
            android:name=".PreferencesActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!--
          Push notifications (Sprint 0 decision, audit finding C3): the FCM
          client is intentionally REMOVED from this build. firebase-messaging
          was previously shipped without google-services.json, i.e. dead code
          plus a latent crash class on Play-Services-less devices. Re-add with
          full configuration when server push is actually enabled.
        -->

    </application>

</manifest>
