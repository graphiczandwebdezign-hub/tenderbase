fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
alembic==1.19.1
pydantic==2.13.5
pydantic-settings==2.15.0
psycopg2-binary==2.9.12
httpx==0.28.1
APScheduler==3.11.3
slowapi==0.1.10
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
jinja2==3.1.6
python-multipart==0.0.20
email-validator==2.2.0
# FCM (Firebase Admin) is OPTIONAL and installed separately (see
# requirements-fcm.txt). It pulls heavy native deps (grpcio) that can exceed
# free-tier build limits. The FCM client imports it lazily, so the app runs
# fine without it — install it only when you enable push notifications.
# Dev / test
pytest==9.1.1
pytest-asyncio==0.25.3
aiosqlite==0.20.0
